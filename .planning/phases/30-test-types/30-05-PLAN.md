---
phase: 30-test-types
plan: 05
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - functions/api/routes/events/events.test.ts
  - functions/api/routes/seasons.test.ts
  - functions/api/routes/outreach.test.ts
  - functions/api/routes/logistics.test.ts
  - functions/api/routes/locations.test.ts
autonomous: true
requirements:
  - Anti-pattern 3 (Over-Typed Test Mocks)
  - TEST-03 (Backend tests typed)
  - TEST-04 (Hono context typing)

must_haves:
  truths:
    - "All 5 events/logistics test files use MockKysely type"
    - "All 5 files use TestEnv for Hono binding"
    - "Factories used for test data generation"
    - "No 'any' violations remain in migrated files"
  artifacts:
    - path: "functions/api/routes/events/events.test.ts"
      provides: "Events route tests with typed mocks"
    - path: "functions/api/routes/seasons.test.ts"
      provides: "Seasons route tests with typed mocks"
    - path: "functions/api/routes/outreach.test.ts"
      provides: "Outreach route tests with typed mocks"
    - path: "functions/api/routes/logistics.test.ts"
      provides: "Logistics route tests with typed mocks"
    - path: "functions/api/routes/locations.test.ts"
      provides: "Locations route tests with typed mocks"
  key_links:
    - from: "functions/api/routes/events/events.test.ts"
      to: "src/test/types.ts"
      via: "MockKysely, TestEnv imports"
      pattern: "import.*MockKysely|import.*TestEnv"
    - from: "functions/api/routes/logistics tests"
      to: "src/test/factories/eventFactory.ts"
      via: "createMockEvent, createMockLocation imports"
      pattern: "import.*createMock.*from.*eventFactory"
---

<objective>
Migrate 5 events/logistics backend test files to use MockKysely and TestEnv types, eliminating `any` violations in mockDb declarations and Hono context typing.

Purpose: Apply type infrastructure to events and logistics tests, which involve scheduling, locations, and outreach tracking.

Output: 5 test files with MockKysely mockDb, Hono<TestEnv>, and typed middleware mocks.
</object>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-types/30-CONTEXT.md
@.planning/phases/30-test-types/30-01-PLAN.md
@.planning/phases/30-test-types/30-02-PLAN.md
@src/test/types.ts
@src/test/factories/eventFactory.ts
@src/test/factories/logisticsFactory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate events.test.ts to typed mocks</name>
  <files>functions/api/routes/events/events.test.ts</files>
  <action>
Update functions/api/routes/events/events.test.ts:

1. **Add imports**:
   - Import { MockKysely, TestEnv } from "~/src/test/types"
   - Import createMockEvent, createMockLocation from "~/src/test/factories/eventFactory"

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `let testApp: Hono<TestEnv>;`

4. **Fix middleware mocks**: Use `c: Context<TestEnv>` instead of `: any`

5. **Remove any casts**: Replace all `as any` with proper types

6. **Use typed factories**: createMockEvent for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/events/events.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/events/events.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate seasons.test.ts to typed mocks</name>
  <files>functions/api/routes/seasons.test.ts</files>
  <action>
Apply migration pattern to seasons.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/seasons.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/seasons.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate outreach.test.ts to typed mocks</name>
  <files>functions/api/routes/outreach.test.ts</files>
  <action>
Apply migration pattern to outreach.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockOutreach

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockOutreach for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/outreach.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/outreach.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate logistics.test.ts to typed mocks</name>
  <files>functions/api/routes/logistics.test.ts</files>
  <action>
Apply migration pattern to logistics.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockOutreach, createMockLocation

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockOutreach, createMockLocation for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/logistics.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/logistics.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate locations.test.ts to typed mocks</name>
  <files>functions/api/routes/locations.test.ts</files>
  <action>
Apply migration pattern to locations.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockLocation

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockLocation for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/locations.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/locations.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → logistics API | Test mocks simulate logistics CRUD operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | I | Logistics mock types | Accept | Test-only. MockKysely ensures DB mock structure matches real API. |

**Note:** Test mocks have no runtime security impact.
</threat_model>

<verification>
- TypeScript compiles without errors
- All 5 test files pass individually
- `grep -c ": any" functions/api/routes/events/events.test.ts functions/api/routes/seasons.test.ts functions/api/routes/outreach.test.ts functions/api/routes/logistics.test.ts functions/api/routes/locations.test.ts` returns 0
</verification>

<success_criteria>
1. All 5 files migrated
2. Zero `: any` violations in migrated files
3. MockKysely and TestEnv types used throughout
4. All tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-types/30-05-SUMMARY.md`
</output>
