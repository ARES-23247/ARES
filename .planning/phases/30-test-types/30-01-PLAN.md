---
phase: 30-test-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/types.ts
  - src/test/utils.tsx
autonomous: true
requirements:
  - Anti-pattern 3 (Over-Typed Test Mocks)
  - TEST-01 (Typed mock database utilities)
  - TEST-02 (Typed factory functions)
  - TEST-03 (Backend tests typed)
  - TEST-04 (Hono context typing)

must_haves:
  truths:
    - "Test types are defined in src/test/types.ts"
    - "MockKysely type provides fluent chaining without any"
    - "TestEnv type defines Hono environment binding"
    - "MockExecutionContext type removes any from utils.tsx"
    - "createMockExpressionBuilder returns typed interface"
  artifacts:
    - path: "src/test/types.ts"
      provides: "Shared test type definitions"
      exports: ["MockKysely", "TestEnv", "MockExecutionContext", "MockExpressionBuilder"]
    - path: "src/test/utils.tsx"
      provides: "Test utilities with zero any violations"
      exports: ["mockExecutionContext", "createMockExpressionBuilder", "flushWaitUntil", "renderWithProviders"]
  key_links:
    - from: "functions/api/routes/*.test.ts"
      to: "src/test/types.ts"
      via: "import { MockKysely, TestEnv }"
      pattern: "import.*MockKysely|import.*TestEnv"
    - from: "src/test/factories/*.ts"
      to: "src/test/types.ts"
      via: "MockExecutionContext import"
      pattern: "import.*MockExecutionContext"
---

<objective>
Create shared test type infrastructure that provides type-safe mocks for Kysely databases, Hono contexts, and Cloudflare Workers ExecutionContext.

Purpose: Eliminate the 3 `any` violations in src/test/utils.tsx and provide the foundation for migrating 126 backend test violations.

Output: src/test/types.ts with MockKysely, TestEnv, MockExecutionContext, MockExpressionBuilder types; updated utils.tsx using these types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-types/30-CONTEXT.md
@.planning/phases/30-test-types/30-RESEARCH.md
@.planning/phases/29-contract-inference/29-01-SUMMARY.md
@shared/types/database.ts
@shared/types/api.ts
@src/test/utils.tsx
</context>

<interfaces>
<!-- Key types from shared infrastructure that tests need -->

From shared/types/database.ts:
```typescript
export type D1Row<T extends keyof DB> = Selectable<DB[T]>;
export type SelectableRow<T extends keyof DB> = Selectable<DB[T]>;
export type InsertableRow<T extends keyof DB> = Insertable<DB[T]>;
export type UpdateableRow<T extends keyof DB> = Updateable<DB[T]>;
```

From shared/types/api.ts:
```typescript
export type AppEnv = MiddlewareAppEnv;
export type HonoContext = Context<AppEnv>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/test/types.ts with shared test type definitions</name>
  <files>src/test/types.ts</files>
  <action>
Create new file src/test/types.ts with the following types:

1. **MockKysely<T>** - Partial Kysely interface for database mocks
   - Generic over table name T (extends keyof DB)
   - Methods: selectFrom, insertInto, updateTable, deleteFrom (all vi.fn mock return type)
   - Methods: execute, executeTakeFirst (vi.fn mock return type)
   - Chain methods: innerJoin, leftJoin, select, selectAll, where, orderBy, limit, offset, groupBy
   - Each method returns vi.fn().mockReturnThis() for fluent chaining
   - Use Pick from Kysely<DB> for base methods, add mock-specific methods

2. **TestEnv** - Hono environment binding for tests
   - Variables: db (MockKysely<any>), user ({ id: string; email: string; role: string })
   - Bindings: DEV_BYPASS (string), DB (D1Database)
   - Import Context from Hono for environment typing

3. **MockExecutionContext** - Cloudflare Workers ExecutionContext mock
   - waitUntil: vi.fn mock that accepts Promise<unknown>
   - passThroughOnException: vi.fn mock

4. **MockExpressionBuilder** - Kysely ExpressionBuilder mock interface
   - Fluent chain methods: or, and, val (all mockReturnThis)
   - fn object with: count, sum, max, min, coalesce (each returns asMock with as method)
   - case object with: when, and, then, else, end (all mockReturnThis)
   - selectFrom, select, where, execute methods for nested query building

5. **Export barrel** - Export all types for easy importing

Constraints:
- No `any` in type definitions (use unknown where type cannot be known)
- Use vi.fn mock types (ReturnType<typeof vi.fn>)
- Keep generic parameters ≤2 (per Anti-pattern 1)
- Type definitions ≤5 lines each where possible
  </action>
  <verify>
    <automated>grep -c "any" src/test/types.ts | grep -q "^0$" && grep -q "export.*MockKysely" src/test/types.ts && grep -q "export.*TestEnv" src/test/types.ts && grep -q "export.*MockExecutionContext" src/test/types.ts</automated>
  </verify>
  <done>
    - src/test/types.ts exists with all 4 type exports
    - Zero `any` violations in the file
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update src/test/utils.tsx to use new types</name>
  <files>src/test/utils.tsx</files>
  <action>
Update src/test/utils.tsx to eliminate all 3 `any` violations:

1. **mockExecutionContext** - Change from `: any` to `: MockExecutionContext`
   - Import type from src/test/types.ts
   - Remove eslint-disable comment for this export

2. **createMockExpressionBuilder** - Add return type `MockExpressionBuilder`
   - Import type from src/test/types.ts
   - Change function signature to include return type annotation
   - Remove `: any` from ebMock variable

3. **flushWaitUntil** - Fix parameter types
   - Change calls parameter type from `any[]` to use proper vi mock call type
   - Use vi.MockedFunction<Promise<unknown>> pattern for type safety

4. **Remove file-level eslint-disable** - Delete the `/* eslint-disable @typescript-eslint/no-explicit-any */` at top of file

Note: The existing ModalContext mock and test utilities (renderWithProviders, createTestQueryClient) are fine and should not be changed.
  </action>
  <verify>
    <automated>grep -c "any" src/test/utils.tsx | grep -q "^0$" && grep -q "import.*MockExecutionContext\|import.*types" src/test/utils.tsx</automated>
  </verify>
  <done>
    - src/test/utils.tsx has zero `any` violations
    - MockExecutionContext type imported and used
    - MockExpressionBuilder type imported and used
    - File-level eslint-disable removed
    - All imports resolve correctly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → runtime | Test mocks simulate external dependencies (Kysely, Cloudflare Workers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | T | Mock types | Accept | Test-only types, not used in production code. Type safety prevents mock API mismatches. |
| T-30-02 | S | ExecutionContext mock | Mitigate | Ensure waitUntil mock preserves Promise semantics for async background task testing. |
| T-30-03 | I | MockKysely fluent chain | Mitigate | MockExpressionBuilder provides complete Kysely API surface to prevent runtime "method not found" in tests. |

**Note:** Test types are compile-time only and do not affect runtime security. The primary risk is test fragility if mocks don't match real APIs, which proper typing mitigates.
</threat_model>

<verification>
- TypeScript compiles without errors
- `npm run test -- src/test/utils.tsx` passes
- `grep -r "as any\|: any\|<any>" src/test/types.ts src/test/utils.tsx` returns no matches (excluding comments)
- All exported types can be imported in a test file
</verification>

<success_criteria>
1. src/test/types.ts exists with MockKysely, TestEnv, MockExecutionContext, MockExpressionBuilder exports
2. src/test/utils.tsx has zero `any` violations
3. TypeScript compiles without errors
4. All existing tests still pass (no runtime behavior changes)
5. No eslint-disable comments remain in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-types/30-01-SUMMARY.md`
</output>
