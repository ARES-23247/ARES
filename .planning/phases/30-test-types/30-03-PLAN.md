---
phase: 30-test-types
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - functions/api/routes/auth.test.ts
  - functions/api/routes/users.test.ts
  - functions/api/routes/profiles.test.ts
  - functions/api/routes/badges.test.ts
  - functions/api/routes/_profileUtils.test.ts
autonomous: true
requirements:
  - Anti-pattern 3 (Over-Typed Test Mocks)
  - TEST-03 (Backend tests typed)
  - TEST-04 (Hono context typing)

must_haves:
  truths:
    - "All 5 auth/core test files use MockKysely type"
    - "All 5 files use TestEnv for Hono binding"
    - "Middleware mocks use proper context types"
    - "No 'any' violations remain in migrated files"
  artifacts:
    - path: "functions/api/routes/auth.test.ts"
      provides: "Auth route tests with typed mocks"
    - path: "functions/api/routes/users.test.ts"
      provides: "User route tests with typed mocks"
    - path: "functions/api/routes/profiles.test.ts"
      provides: "Profile route tests with typed mocks"
    - path: "functions/api/routes/badges.test.ts"
      provides: "Badge route tests with typed mocks"
    - path: "functions/api/routes/_profileUtils.test.ts"
      provides: "Profile utils tests with typed mocks"
  key_links:
    - from: "functions/api/routes/auth.test.ts"
      to: "src/test/types.ts"
      via: "MockKysely, TestEnv imports"
      pattern: "import.*MockKysely|import.*TestEnv"
    - from: "functions/api/routes/badges.test.ts"
      to: "src/test/factories/"
      via: "Factory imports for test data"
      pattern: "import.*createMock.*from.*factories"
---

<objective>
Migrate 5 auth/core backend test files to use MockKysely and TestEnv types, eliminating `any` violations in mockDb declarations and Hono context typing.

Purpose: Apply type infrastructure from Plan 01 to the first batch of backend tests, establishing the migration pattern for remaining tests.

Output: 5 test files with MockKysely mockDb, Hono<TestEnv>, and typed middleware mocks.
</object>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-types/30-CONTEXT.md
@.planning/phases/30-test-types/30-01-PLAN.md
@.planning/phases/30-test-types/30-02-PLAN.md
@src/test/types.ts
@src/test/factories/userFactory.ts
@src/test/factories/authFactory.ts
@functions/api/routes/auth.test.ts
@functions/api/routes/badges.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate auth.test.ts to typed mocks</name>
  <files>functions/api/routes/auth.test.ts</files>
  <action>
Update functions/api/routes/auth.test.ts:

1. **Add imports** at top of file:
   - Import { MockKysely, TestEnv, MockExecutionContext } from "~/src/test/types"
   - Import createMockSession from "~/src/test/factories/authFactory"

2. **Update mockDb declaration**:
   - Change `let mockDb: any;` to `let mockDb: MockKysely;`

3. **Update Hono app typing**:
   - Change `let testApp: Hono<any>;` to `let testApp: Hono<TestEnv>;`
   - Or for router.request calls: update env parameter to use proper type

4. **Fix middleware mock types**:
   - In vi.mock for middleware, change `(c: any, next: any)` to use proper types
   - Use `c: Context<TestEnv>` for context parameter

5. **Update env parameter** in router.request calls:
   - Change `{ DB: {}, DEV_BYPASS: "true" } as any` to match TestEnv.Bindings

6. **Remove `as any` casts** from mocked values:
   - Use proper type assertions or vi.mocked() pattern

7. **Fix test response assertions**:
   - Change `as any` on response.json() to proper type or use type assertion only for specific fields
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/auth.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/auth.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely type
    - testApp uses Hono<TestEnv> or proper type
    - Middleware mocks use Context<TestEnv>
    - No `: any` or `as any` (excluding comments)
    - All tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate users.test.ts to typed mocks</name>
  <files>functions/api/routes/users.test.ts</files>
  <action>
Apply the same migration pattern as Task 1 to users.test.ts:

1. **Add imports**: MockKysely, TestEnv from "~/src/test/types"

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: Use `Context<TestEnv>` instead of `: any`

5. **Remove any casts**: Replace `as any` with proper types

6. **Update response assertions**: Use proper type assertions
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/users.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/users.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely type
    - Hono<TestEnv> used throughout
    - No `: any` or `as any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate profiles.test.ts to typed mocks</name>
  <files>functions/api/routes/profiles.test.ts</files>
  <action>
Apply migration pattern to profiles.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockProfile

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockProfile for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/profiles.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/profiles.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate badges.test.ts to typed mocks</name>
  <files>functions/api/routes/badges.test.ts</files>
  <action>
Apply migration pattern to badges.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockBadge, createMockUser

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>` in vi.mock

5. **Remove any casts**: Replace all `as any` with proper types

6. **Use typed factories**: createMockBadge, createMockUser for test data

Note: badges.test.ts has 15+ test cases. Focus on type changes, not behavior.
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/badges.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/badges.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate _profileUtils.test.ts to typed mocks</name>
  <files>functions/api/routes/_profileUtils.test.ts</files>
  <action>
Apply migration pattern to _profileUtils.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockProfile, createMockUser

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockProfile for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/_profileUtils.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/_profileUtils.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → middleware | Test mocks simulate authentication and authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | S | Auth test mocks | Accept | Test-only. MockKysely ensures DB mock structure matches real API. |
| T-30-07 | I | Middleware bypass | Accept | Tests explicitly bypass auth for isolated testing. Not a production concern. |

**Note:** Test mocks have no runtime security impact. Proper typing ensures tests accurately validate production code.
</threat_model>

<verification>
- TypeScript compiles without errors
- `npm run test -- functions/api/routes/auth.test.ts` passes
- `npm run test -- functions/api/routes/users.test.ts` passes
- `npm run test -- functions/api/routes/profiles.test.ts` passes
- `npm run test -- functions/api/routes/badges.test.ts` passes
- `npm run test -- functions/api/routes/_profileUtils.test.ts` passes
- `grep -c ": any" functions/api/routes/auth.test.ts functions/api/routes/users.test.ts functions/api/routes/profiles.test.ts functions/api/routes/badges.test.ts functions/api/routes/_profileUtils.test.ts` returns 0
</verification>

<success_criteria>
1. All 5 files migrated
2. Zero `: any` violations in migrated files
3. MockKysely and TestEnv types used throughout
4. All tests still pass
5. Pattern established for remaining migrations
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-types/30-03-SUMMARY.md`
</output>
