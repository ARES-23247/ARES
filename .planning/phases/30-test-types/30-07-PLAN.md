---
phase: 30-test-types
plan: 07
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - functions/api/routes/github.test.ts
  - functions/api/routes/githubWebhook.test.ts
  - functions/api/routes/zulip.test.ts
  - functions/api/routes/zulipWebhook.test.ts
  - functions/api/routes/tba.test.ts
  - functions/api/routes/points.test.ts
  - functions/api/routes/awards.test.ts
  - functions/api/routes/judges.test.ts
  - functions/api/routes/inquiries.test.ts
  - functions/api/routes/communications.test.ts
  - functions/api/routes/entities.test.ts
  - functions/api/routes/analytics.test.ts
autonomous: true
requirements:
  - Anti-pattern 3 (Over-Typed Test Mocks)
  - TEST-03 (Backend tests typed)
  - TEST-04 (Hono context typing)

must_haves:
  truths:
    - "All 12 integration/misc test files use MockKysely type"
    - "All 12 files use TestEnv for Hono binding"
    - "No 'any' violations remain in migrated files"
  artifacts:
    - path: "functions/api/routes/github.test.ts"
      provides: "GitHub route tests with typed mocks"
    - path: "functions/api/routes/githubWebhook.test.ts"
      provides: "GitHub webhook tests with typed mocks"
    - path: "functions/api/routes/zulip.test.ts"
      provides: "Zulip route tests with typed mocks"
    - path: "functions/api/routes/zulipWebhook.test.ts"
      provides: "Zulip webhook tests with typed mocks"
    - path: "functions/api/routes/tba.test.ts"
      provides: "TBA route tests with typed mocks"
    - path: "functions/api/routes/points.test.ts"
      provides: "Points route tests with typed mocks"
    - path: "functions/api/routes/awards.test.ts"
      provides: "Awards route tests with typed mocks"
    - path: "functions/api/routes/judges.test.ts"
      provides: "Judges route tests with typed mocks"
    - path: "functions/api/routes/inquiries.test.ts"
      provides: "Inquiries route tests with typed mocks"
    - path: "functions/api/routes/communications.test.ts"
      provides: "Communications route tests with typed mocks"
    - path: "functions/api/routes/entities.test.ts"
      provides: "Entities route tests with typed mocks"
    - path: "functions/api/routes/analytics.test.ts"
      provides: "Analytics route tests with typed mocks"
  key_links:
    - from: "functions/api/routes/integration tests"
      to: "src/test/types.ts"
      via: "MockKysely, TestEnv imports"
      pattern: "import.*MockKysely|import.*TestEnv"
---

<objective>
Migrate 12 integration/misc backend test files to use MockKysely and TestEnv types, eliminating `any` violations in mockDb declarations and Hono context typing.

Purpose: Apply type infrastructure to integration tests (GitHub, Zulip, TBA webhooks) and miscellaneous routes (points, awards, judges, inquiries, communications, entities, analytics).

Output: 12 test files with MockKysely mockDb, Hono<TestEnv>, and typed middleware mocks.
</object>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-types/30-CONTEXT.md
@.planning/phases/30-test-types/30-01-PLAN.md
@.planning/phases/30-test-types/30-02-PLAN.md
@src/test/types.ts
@src/test/factories/logisticsFactory.ts
@src/test/factories/systemFactory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate github.test.ts to typed mocks</name>
  <files>functions/api/routes/github.test.ts</files>
  <action>
Update functions/api/routes/github.test.ts:

1. **Add imports**: { MockKysely, TestEnv } from "~/src/test/types"

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `let testApp: Hono<TestEnv>;`

4. **Fix middleware mocks**: `c: Context<TestEnv>` instead of `: any`

5. **Remove any casts**: Replace all `as any` with proper types
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/github.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/github.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate githubWebhook.test.ts to typed mocks</name>
  <files>functions/api/routes/githubWebhook.test.ts</files>
  <action>
Apply migration pattern to githubWebhook.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/githubWebhook.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/githubWebhook.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate zulip.test.ts to typed mocks</name>
  <files>functions/api/routes/zulip.test.ts</files>
  <action>
Apply migration pattern to zulip.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/zulip.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/zulip.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate zulipWebhook.test.ts to typed mocks</name>
  <files>functions/api/routes/zulipWebhook.test.ts</files>
  <action>
Apply migration pattern to zulipWebhook.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/zulipWebhook.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/zulipWebhook.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate tba.test.ts to typed mocks</name>
  <files>functions/api/routes/tba.test.ts</files>
  <action>
Apply migration pattern to tba.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/tba.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/tba.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 6: Migrate points.test.ts to typed mocks</name>
  <files>functions/api/routes/points.test.ts</files>
  <action>
Apply migration pattern to points.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/points.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/points.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 7: Migrate awards.test.ts to typed mocks</name>
  <files>functions/api/routes/awards.test.ts</files>
  <action>
Apply migration pattern to awards.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockAward

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockAward for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/awards.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/awards.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 8: Migrate judges.test.ts to typed mocks</name>
  <files>functions/api/routes/judges.test.ts</files>
  <action>
Apply migration pattern to judges.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/judges.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/judges.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 9: Migrate inquiries.test.ts to typed mocks</name>
  <files>functions/api/routes/inquiries.test.ts</files>
  <action>
Apply migration pattern to inquiries.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockInquiry

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockInquiry for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/inquiries.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/inquiries.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 10: Migrate communications.test.ts to typed mocks</name>
  <files>functions/api/routes/communications.test.ts</files>
  <action>
Apply migration pattern to communications.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/communications.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/communications.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 11: Migrate entities.test.ts to typed mocks</name>
  <files>functions/api/routes/entities.test.ts</files>
  <action>
Apply migration pattern to entities.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/entities.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/entities.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 12: Migrate analytics.test.ts to typed mocks</name>
  <files>functions/api/routes/analytics.test.ts</files>
  <action>
Apply migration pattern to analytics.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockAnalytics

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockAnalytics for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/analytics.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/analytics.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → integration APIs | Test mocks simulate external service integrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-12 | S | Integration mock types | Accept | Test-only. MockKysely ensures DB mock structure matches real API. |
| T-30-13 | I | Webhook validation | Accept | Tests use mock payloads. No production exposure. |

**Note:** Test mocks have no runtime security impact.
</threat_model>

<verification>
- TypeScript compiles without errors
- All 12 test files pass individually
- `grep -c ": any" functions/api/routes/github.test.ts functions/api/routes/githubWebhook.test.ts functions/api/routes/zulip.test.ts functions/api/routes/zulipWebhook.test.ts functions/api/routes/tba.test.ts functions/api/routes/points.test.ts functions/api/routes/awards.test.ts functions/api/routes/judges.test.ts functions/api/routes/inquiries.test.ts functions/api/routes/communications.test.ts functions/api/routes/entities.test.ts functions/api/routes/analytics.test.ts` returns 0
</verification>

<success_criteria>
1. All 12 files migrated
2. Zero `: any` violations in migrated files
3. MockKysely and TestEnv types used throughout
4. All tests still pass
5. All 26 backend test files migrated (plans 03-07 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-types/30-07-SUMMARY.md`
</output>
