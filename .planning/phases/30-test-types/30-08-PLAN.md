---
phase: 30-test-types
plan: 08
type: execute
wave: 4
depends_on: [30-01]
files_modified:
  - tests/e2e/media.spec.ts
  - tests/e2e/kanban.spec.ts
autonomous: true
requirements:
  - Anti-pattern 3 (Over-Typed Test Mocks)
  - TEST-03 (Backend tests typed)

must_haves:
  truths:
    - "E2E test files use typed interfaces for mock data"
    - "No 'any' violations remain in E2E tests"
  artifacts:
    - path: "tests/e2e/media.spec.ts"
      provides: "Media E2E tests with typed mocks"
    - path: "tests/e2e/kanban.spec.ts"
      provides: "Kanban E2E tests with typed mocks"
  key_links:
    - from: "tests/e2e/media.spec.ts"
      to: "shared/types/database.ts"
      via: "Media interface or D1Row type"
      pattern: "import.*D1Row|import.*Media"
---

<objective>
Fix the 2 `any` violations in E2E test files by creating typed interfaces for mock data arrays.

Purpose: Complete the final `any` elimination in E2E tests (media.spec.ts and kanban.spec.ts each have one `any[]` violation).

Output: 2 E2E test files with typed mock data interfaces.
</object>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-types/30-CONTEXT.md
@.planning/phases/30-test-types/30-01-PLAN.md
@tests/e2e/media.spec.ts
@tests/e2e/kanban.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix media.spec.ts any violation</name>
  <files>tests/e2e/media.spec.ts</files>
  <action>
Update tests/e2e/media.spec.ts:

1. **Locate the violation** at line 77: `const mockMediaItems: any[] = [];`

2. **Create MediaItem interface** (if not exists):
   ```typescript
   interface MediaItem {
     key: string;
     url: string;
     size: number;
     type: string;
     folder: string;
     uploaded_at: string;
   }
   ```

3. **Update declaration**:
   - Change `const mockMediaItems: any[] = [];`
   - To `const mockMediaItems: MediaItem[] = [];`

4. **Verify mock data** in test usage matches interface structure
  </action>
  <verify>
    <automated>grep -c ": any\|<any>" tests/e2e/media.spec.ts | grep -q "^0$" && grep -q "MediaItem\[\]" tests/e2e/media.spec.ts</automated>
  </verify>
  <done>
    - mockMediaItems uses MediaItem[] type
    - No `: any` violations in media.spec.ts
    - E2E tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix kanban.spec.ts any violation</name>
  <files>tests/e2e/kanban.spec.ts</files>
  <action>
Update tests/e2e/kanban.spec.ts:

1. **Locate the violation** at line 65: `const mockTasks: any[] = [];`

2. **Create TaskItem interface** (if not exists):
   ```typescript
   interface TaskItem {
     id: string;
     title: string;
     description?: string;
     status: string;
     assignee_id?: string;
     column_id: string;
     position: number;
   }
   ```
   (Adjust fields based on actual task schema in the test)

3. **Update declaration**:
   - Change `const mockTasks: any[] = [];`
   - To `const mockTasks: TaskItem[] = [];`

4. **Verify mock data** in test usage matches interface structure
  </action>
  <verify>
    <automated>grep -c ": any\|<any>" tests/e2e/kanban.spec.ts | grep -q "^0$" && grep -q "TaskItem\[\]" tests/e2e/kanban.spec.ts</automated>
  </verify>
  <done>
    - mockTasks uses TaskItem[] type
    - No `: any` violations in kanban.spec.ts
    - E2E tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → application | Playwright simulates user interactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-14 | I | E2E mock data | Accept | E2E tests use mock data for isolation. No production exposure. |

**Note:** E2E test typing has no runtime security impact.
</threat_model>

<verification>
- TypeScript compiles without errors
- `npm run test:e2e -- tests/e2e/media.spec.ts` passes
- `npm run test:e2e -- tests/e2e/kanban.spec.ts` passes
- `grep -c ": any\|<any>" tests/e2e/media.spec.ts tests/e2e/kanban.spec.ts` returns 0
</verification>

<success_criteria>
1. Both E2E test files fixed
2. Zero `any` violations in E2E tests
3. E2E tests still pass
4. All 131 test `any` violations eliminated across the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-types/30-08-SUMMARY.md`
</output>
