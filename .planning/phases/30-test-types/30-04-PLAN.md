---
phase: 30-test-types
plan: 04
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - functions/api/routes/docs.test.ts
  - functions/api/routes/posts.test.ts
  - functions/api/routes/comments.test.ts
  - functions/api/routes/media.test.ts
autonomous: true
requirements:
  - Anti-pattern 3 (Over-Typed Test Mocks)
  - TEST-03 (Backend tests typed)
  - TEST-04 (Hono context typing)

must_haves:
  truths:
    - "All 4 content test files use MockKysely type"
    - "All 4 files use TestEnv for Hono binding"
    - "Factories used for test data generation"
    - "No 'any' violations remain in migrated files"
  artifacts:
    - path: "functions/api/routes/docs.test.ts"
      provides: "Docs route tests with typed mocks"
    - path: "functions/api/routes/posts.test.ts"
      provides: "Posts route tests with typed mocks"
    - path: "functions/api/routes/comments.test.ts"
      provides: "Comments route tests with typed mocks"
    - path: "functions/api/routes/media.test.ts"
      provides: "Media route tests with typed mocks"
  key_links:
    - from: "functions/api/routes/docs.test.ts"
      to: "src/test/types.ts"
      via: "MockKysely, TestEnv imports"
      pattern: "import.*MockKysely|import.*TestEnv"
    - from: "functions/api/routes/content tests"
      to: "src/test/factories/contentFactory.ts"
      via: "createMockPost, createMockDoc imports"
      pattern: "import.*createMock.*from.*contentFactory"
---

<objective>
Migrate 4 content/docs backend test files to use MockKysely and TestEnv types, eliminating `any` violations in mockDb declarations and Hono context typing.

Purpose: Apply type infrastructure to content-related tests, which have complex database query patterns (leftJoin, distinct, onConflict).

Output: 4 test files with MockKysely mockDb, Hono<TestEnv>, and typed middleware mocks.
</object>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-types/30-CONTEXT.md
@.planning/phases/30-test-types/30-01-PLAN.md
@.planning/phases/30-test-types/30-02-PLAN.md
@src/test/types.ts
@src/test/factories/contentFactory.ts
@src/test/factories/userFactory.ts
@functions/api/routes/docs.test.ts
@functions/api/routes/comments.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate docs.test.ts to typed mocks</name>
  <files>functions/api/routes/docs.test.ts</files>
  <action>
Update functions/api/routes/docs.test.ts:

1. **Add imports**:
   - Import { MockKysely, TestEnv } from "~/src/test/types"
   - Import createMockDoc, createMockUser from "~/src/test/factories"

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `let testApp: Hono<TestEnv>;`

4. **Fix middleware mocks**: Use `c: Context<TestEnv>` instead of `: any`

5. **Remove any casts**:
   - Replace `as any` on getSessionUser mock return
   - Use vi.mocked() pattern for typed mocks

6. **Update complex mockDb methods**:
   - Ensure onConflict mock handles callback parameter properly
   - Keep getExecutor mock (complex Kysely internal)

Note: docs.test.ts has complex query mocks (leftJoin, distinct, onConflict). Focus on type annotations, not mock behavior.
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/docs.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/docs.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate posts.test.ts to typed mocks</name>
  <files>functions/api/routes/posts.test.ts</files>
  <action>
Apply migration pattern to posts.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockPost, createMockComment

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockPost for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/posts.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/posts.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate comments.test.ts to typed mocks</name>
  <files>functions/api/routes/comments.test.ts</files>
  <action>
Apply migration pattern to comments.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockComment, createMockUser

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockComment, createMockUser for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/comments.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/comments.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate media.test.ts to typed mocks</name>
  <files>functions/api/routes/media.test.ts</files>
  <action>
Apply migration pattern to media.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockMedia

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockMedia for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/media.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/media.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → content API | Test mocks simulate content CRUD operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | I | Content mock types | Accept | Test-only. MockKysely ensures DB mock structure matches real API. |
| T-30-09 | T | Content injection | Accept | Tests use mock data. No production exposure. |

**Note:** Test mocks have no runtime security impact.
</threat_model>

<verification>
- TypeScript compiles without errors
- `npm run test -- functions/api/routes/docs.test.ts` passes
- `npm run test -- functions/api/routes/posts.test.ts` passes
- `npm run test -- functions/api/routes/comments.test.ts` passes
- `npm run test -- functions/api/routes/media.test.ts` passes
- `grep -c ": any" functions/api/routes/docs.test.ts functions/api/routes/posts.test.ts functions/api/routes/comments.test.ts functions/api/routes/media.test.ts` returns 0
</verification>

<success_criteria>
1. All 4 files migrated
2. Zero `: any` violations in migrated files
3. MockKysely and TestEnv types used throughout
4. All tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-types/30-04-SUMMARY.md`
</output>
