---
phase: 30-test-types
plan: 06
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - functions/api/routes/sponsors.test.ts
  - functions/api/routes/finance.test.ts
  - functions/api/routes/store.test.ts
  - functions/api/routes/tasks.test.ts
  - functions/api/routes/settings.test.ts
  - functions/api/routes/notifications.test.ts
autonomous: true
requirements:
  - Anti-pattern 3 (Over-Typed Test Mocks)
  - TEST-03 (Backend tests typed)
  - TEST-04 (Hono context typing)

must_haves:
  truths:
    - "All 6 admin/operations test files use MockKysely type"
    - "All 6 files use TestEnv for Hono binding"
    - "Factories used for test data generation"
    - "No 'any' violations remain in migrated files"
  artifacts:
    - path: "functions/api/routes/sponsors.test.ts"
      provides: "Sponsors route tests with typed mocks"
    - path: "functions/api/routes/finance.test.ts"
      provides: "Finance route tests with typed mocks"
    - path: "functions/api/routes/store.test.ts"
      provides: "Store route tests with typed mocks"
    - path: "functions/api/routes/tasks.test.ts"
      provides: "Tasks route tests with typed mocks"
    - path: "functions/api/routes/settings.test.ts"
      provides: "Settings route tests with typed mocks"
    - path: "functions/api/routes/notifications.test.ts"
      provides: "Notifications route tests with typed mocks"
  key_links:
    - from: "functions/api/routes/sponsors.test.ts"
      to: "src/test/types.ts"
      via: "MockKysely, TestEnv imports"
      pattern: "import.*MockKysely|import.*TestEnv"
    - from: "functions/api/routes/admin tests"
      to: "src/test/factories/logisticsFactory.ts"
      via: "createMockSponsor, createMockNotification imports"
      pattern: "import.*createMock.*from.*logisticsFactory"
---

<objective>
Migrate 6 admin/operations backend test files to use MockKysely and TestEnv types, eliminating `any` violations in mockDb declarations and Hono context typing.

Purpose: Apply type infrastructure to admin and operations tests (sponsors, finance, store, tasks, settings, notifications).

Output: 6 test files with MockKysely mockDb, Hono<TestEnv>, and typed middleware mocks.
</object>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-types/30-CONTEXT.md
@.planning/phases/30-test-types/30-01-PLAN.md
@.planning/phases/30-test-types/30-02-PLAN.md
@src/test/types.ts
@src/test/factories/logisticsFactory.ts
@src/test/factories/systemFactory.ts
@src/test/factories/authFactory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sponsors.test.ts to typed mocks</name>
  <files>functions/api/routes/sponsors.test.ts</files>
  <action>
Update functions/api/routes/sponsors.test.ts:

1. **Add imports**:
   - Import { MockKysely, TestEnv } from "~/src/test/types"
   - Import createMockSponsor from "~/src/test/factories/logisticsFactory"

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `let testApp: Hono<TestEnv>;`

4. **Fix middleware mocks**: Use `c: Context<TestEnv>` instead of `: any`

5. **Remove any casts**: Replace all `as any` with proper types

6. **Use typed factories**: createMockSponsor for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/sponsors.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/sponsors.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate finance.test.ts to typed mocks</name>
  <files>functions/api/routes/finance.test.ts</files>
  <action>
Apply migration pattern to finance.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/finance.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/finance.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate store.test.ts to typed mocks</name>
  <files>functions/api/routes/store.test.ts</files>
  <action>
Apply migration pattern to store.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/store.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/store.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate tasks.test.ts to typed mocks</name>
  <files>functions/api/routes/tasks.test.ts</files>
  <action>
Apply migration pattern to tasks.test.ts:

1. **Add imports**: MockKysely, TestEnv

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/tasks.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/tasks.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate settings.test.ts to typed mocks</name>
  <files>functions/api/routes/settings.test.ts</files>
  <action>
Apply migration pattern to settings.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockSettings

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockSettings for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/settings.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/settings.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 6: Migrate notifications.test.ts to typed mocks</name>
  <files>functions/api/routes/notifications.test.ts</files>
  <action>
Apply migration pattern to notifications.test.ts:

1. **Add imports**: MockKysely, TestEnv, createMockNotification

2. **Update mockDb**: `let mockDb: MockKysely;`

3. **Update Hono typing**: `Hono<TestEnv>`

4. **Fix middleware mocks**: `Context<TestEnv>`

5. **Remove any casts**

6. **Use typed factories**: createMockNotification for test data
  </action>
  <verify>
    <automated>grep -c ": any" functions/api/routes/notifications.test.ts | grep -q "^0$" && grep -q "MockKysely" functions/api/routes/notifications.test.ts</automated>
  </verify>
  <done>
    - mockDb uses MockKysely
    - Hono<TestEnv> used
    - No `: any` violations
    - Tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → admin API | Test mocks simulate admin CRUD operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11 | S | Admin mock types | Accept | Test-only. MockKysely ensures DB mock structure matches real API. |

**Note:** Test mocks have no runtime security impact.
</threat_model>

<verification>
- TypeScript compiles without errors
- All 6 test files pass individually
- `grep -c ": any" functions/api/routes/sponsors.test.ts functions/api/routes/finance.test.ts functions/api/routes/store.test.ts functions/api/routes/tasks.test.ts functions/api/routes/settings.test.ts functions/api/routes/notifications.test.ts` returns 0
</verification>

<success_criteria>
1. All 6 files migrated
2. Zero `: any` violations in migrated files
3. MockKysely and TestEnv types used throughout
4. All tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-types/30-06-SUMMARY.md`
</output>
