---
phase: 27-type-foundation
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified: [functions/api/routes/docs.ts, functions/api/routes/sponsors.ts]
autonomous: true
requirements: [ANTI_PATTERN_1, ANTI_PATTERN_5, FOUNDATION_CHECKLIST]
user_setup: []
must_haves:
  truths:
    - HandlerInput and HandlerOutput types are imported into at least 2 route files
    - Handler function signatures use imported types instead of implicit any
    - No new ESLint violations introduced in modified route files
  artifacts:
    - path: functions/api/routes/docs.ts
      provides: Documentation routes with typed handlers
      imports: ["HandlerInput", "HandlerOutput", "HonoContext"]
      min_lines: 0 (modifying existing)
    - path: functions/api/routes/sponsors.ts
      provides: Sponsor routes with typed handlers
      imports: ["HandlerInput", "HandlerOutput", "HonoContext"]
      min_lines: 0 (modifying existing)
  key_links:
    - from: functions/api/routes/docs.ts
      to: shared/types/api.ts
      via: "import type { HandlerInput, HandlerOutput, HonoContext }"
      pattern: "import.*from.*shared/types"
    - from: functions/api/routes/sponsors.ts
      to: shared/types/api.ts
      via: "import type { HandlerInput, HandlerOutput, HonoContext }"
      pattern: "import.*from.*shared/types"
---

<objective>
Adopt the shared handler types in actual route files to demonstrate the type foundation works in practice and establish the pattern for Phase 28's mass migration.

Purpose: Success Criterion 4 requires that handler types are "defined and imported by at least 2 route files." Plan 27-01 defines the types; this plan proves they work by importing them into two representative route files (docs.ts and sponsors.ts). These routes were chosen because they represent different patterns: docs.ts has many complex handlers, sponsors.ts uses ts-rest contracts.

Output: Two route files (docs.ts, sponsors.ts) with HandlerInput/HandlerOutput imports replacing implicit any signatures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-type-foundation/27-01-PLAN.md
@shared/types/api.ts
@functions/api/routes/docs.ts
@functions/api/routes/sponsors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import HandlerInput/HandlerOutput into docs.ts route handlers</name>
  <files>functions/api/routes/docs.ts</files>
  <action>Modify docs.ts to use shared handler types:

1. Add import: "import type { HandlerInput, HandlerOutput, HonoContext } from '../../../shared/types/api';"
2. Keep existing Context<AppEnv> imports (avoid breaking change)
3. For each handler in docTsRestRouter, add type annotations using imported types:
   - Change "async (_: any, c: Context<AppEnv>)" to "async (_: HandlerInput, c: HonoContext)"
   - Change "async ({ query }: { query: any }, c: Context<AppEnv>)" to "async ({ query }: HandlerInput, c: HonoContext)"
   - Change "async ({ params }: { params: any }, c: Context<AppEnv>)" to "async ({ params }: HandlerInput, c: HonoContext)"
   - Change "async ({ body }: { body: any }, c: Context<AppEnv>)" to "async ({ body }: HandlerInput, c: HonoContext)"
   - Change "async ({ params, body }: { params: any, body: any }, c: Context<AppEnv>)" to "async ({ params, body }: HandlerInput, c: HonoContext)"
4. Do NOT modify handler body logic — only type signatures
5. The "any" return types (e.g., "body: { docs: docs as any[] })" remain — full any elimination is Phase 28 scope

This demonstrates the import pattern without risking route functionality.
</action>
  <verify>
    <automated>grep -c "import type.*HandlerInput.*from.*shared/types" functions/api/routes/docs.ts && npx tsc --noEmit --skipLibCheck functions/api/routes/docs.ts</automated>
  </verify>
  <done>docs.ts imports HandlerInput/HandlerOutput; TypeScript compiles; handler signatures use imported types</done>
</task>

<task type="auto">
  <name>Task 2: Import HandlerInput/HandlerOutput into sponsors.ts route handlers</name>
  <files>functions/api/routes/sponsors.ts</files>
  <action>Modify sponsors.ts to use shared handler types:

1. Add import: "import type { HandlerInput, HandlerOutput, HonoContext } from '../../../shared/types/api';"
2. Keep existing Context<AppEnv> imports
3. For each handler in sponsorHandlers object, add type annotations:
   - Change "async (_: any, c: Context<AppEnv>)" to "async (_: HandlerInput, c: HonoContext)"
   - Change "async ({ params }: { params: any }, c: Context<AppEnv>)" to "async ({ params }: HandlerInput, c: HonoContext)"
   - Change "async ({ body }: { body: any }, c: Context<AppEnv>)" to "async ({ body }: HandlerInput, c: HonoContext)"
4. Do NOT modify handler body logic — only type signatures
5. The "any" return types remain — full any elimination is Phase 28 scope

Verifies the pattern works with ts-rest-integrated routes too.
</action>
  <verify>
    <automated>grep -c "import type.*HandlerInput.*from.*shared/types" functions/api/routes/sponsors.ts && npx tsc --noEmit --skipLibCheck functions/api/routes/sponsors.ts</automated>
  </verify>
  <done>sponsors.ts imports HandlerInput/HandlerOutput; TypeScript compiles; handler signatures use imported types</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs.ts → D1 Database | Handler input crosses into database queries |
| sponsors.ts → D1 Database | Sponsor data queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | I | HandlerInput types | accept | Type-only changes — no new input validation or code paths |
| T-27-05 | D | Route handler signatures | accept | Adding types doesn't change runtime behavior or data disclosure |

These are type-only changes with no runtime impact.
</threat_model>

<verification>
## Overall Phase Checks

1. **Import Verification**: Both route files import HandlerInput/HandlerOutput from shared/types
2. **TypeScript Compilation**: Modified files compile without errors
3. **Handler Signature Count**: Count handlers using imported types (expect ≥2 per file)
4. **ESLint Verification**: No new ESLint violations in modified files

## Commands

```bash
# Verify imports exist in both files
grep "import type.*HandlerInput.*from.*shared/types" functions/api/routes/docs.ts functions/api/routes/sponsors.ts

# Compile both files
npx tsc --noEmit --skipLibCheck functions/api/routes/docs.ts functions/api/routes/sponsors.ts

# Count handlers with typed signatures
grep -c "HandlerInput" functions/api/routes/docs.ts
grep -c "HandlerInput" functions/api/routes/sponsors.ts

# Check ESLint (should not increase violation count)
npx eslint functions/api/routes/docs.ts functions/api/routes/sponsors.ts --format compact || true
```
</verification>

<success_criteria>
1. docs.ts imports { HandlerInput, HandlerOutput, HonoContext } from shared/types/api
2. sponsors.ts imports { HandlerInput, HandlerOutput, HonoContext } from shared/types/api
3. Both files compile without TypeScript errors
4. At least 2 handler signatures per file use HandlerInput instead of "{ any }" inline types
5. No new ESLint violations introduced
</success_criteria>

<output>
After completion, create `.planning/phases/27-type-foundation/27-02-SUMMARY.md` with:
- List of handlers modified in each file
- Verification results (imports, compilation, ESLint)
- Pattern examples for Phase 28 to replicate across remaining routes
- Note: This is a "demo adoption" — full migration to eliminate all any is Phase 28 scope
</output>
