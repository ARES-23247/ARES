---
phase: 29.1-contract-inference-remaining
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/api/routes/judges.ts
  - functions/api/routes/users.ts
autonomous: true
requirements:
  - ANTI_PATTERN_4
  - CONTRACT_INFERENCE

must_haves:
  truths:
    - "judges.ts uses inferred contract types with zero `as any` casts"
    - "users.ts uses inferred contract types with zero `as any` casts"
    - "Both files enable runtime response validation"
    - "Handler signatures use inferred `(input, c)` pattern without explicit type annotations"
  artifacts:
    - path: "functions/api/routes/judges.ts"
      provides: "Judges API with contract inference"
      contains: "s.router(judgeContract, judgesTsRestRouter)"
    - path: "functions/api/routes/users.ts"
      provides: "Users API with contract inference"
      contains: "s.router(userContract, userTsRestRouter)"
  key_links:
    - from: "both route files"
      to: "shared/types/contracts.ts"
      via: "AppRouteInput import"
      pattern: "import.*AppRouteInput.*from.*shared/types"
    - from: "both route files"
      to: "shared/schemas/contracts"
      via: "Contract type inference"
      pattern: "s.router\\(.*Contract, .*Router\\)"
---

<objective>
Migrate judges.ts and users.ts to ts-rest contract inference, eliminating `as any` casts and enabling runtime response validation.

Purpose: Apply the proven Phase 29 migration pattern to judges.ts and users.ts. These files have more complex handler patterns (judges.ts has portfolio caching, users.ts has profile updates) but follow the same contract inference pattern.

Output: Both files using contract inference with zero `as any` casts and response validation enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-contract-inference/29-01-SUMMARY.md
@.planning/phases/29-contract-inference/29-02-SUMMARY.md
@shared/types/contracts.ts
@shared/types/api.ts
@shared/schemas/contracts/judgeContract.ts
@shared/schemas/contracts/userContract.ts
@functions/api/routes/analytics.ts
</context>

<interfaces>
<!-- Key types and contracts from Phase 29 infrastructure -->

From shared/types/contracts.ts:
```typescript
export type { AppRouteImplementation, AppRouteInput, ServerInferResponses, RecursiveRouterObj } from "ts-rest-hono";
```

From shared/schemas/contracts (contracts already exist):
```typescript
export const judgeContract = c.router({ /* ... */ });
export const userContract = c.router({ /* ... */ });
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate judges.ts to contract inference</name>
  <files>
    functions/api/routes/judges.ts
  </files>
  <action>
For judges.ts:

1. Add import: `import type { AppRouteInput } from "../../../shared/types/contracts";`

2. Update all handler signatures to use inferred types (6 handlers: login, portfolio, listCodes, createCode, deleteCode):
   - Change `async ({ body }: { body: any }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Change `async ({ headers }: { headers: any }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Change `async (_: any, c: Context<AppEnv>) =>` to `async (_input, c) =>` (for unused input)
   - Change `async ({ params }: { params: any }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Update destructuring: `const { field } = input.body;` instead of `const { field } = body;`

3. Remove `as any` cast from router setup:
   - Line 31: Change `const judgesTsRestRouter: any = s.router(judgeContract as any, {` to `const judgesTsRestRouter = s.router(judgeContract, {`
   - Line 208: Remove trailing `} as any);` and replace with `});`

4. Enable response validation in createHonoEndpoints call (line 211):
   ```typescript
   createHonoEndpoints(
     judgeContract,
     judgesTsRestRouter,
     judgesRouter,
     {
       responseValidation: true,
       responseValidationErrorHandler: (err, _c) => {
         console.error('[Contract] Response validation failed:', err.cause);
         return { error: { message: 'Internal server error' }, status: 500 };
       }
     }
   );
   ```

5. Run TypeScript check: `npx tsc --noEmit` to verify no type errors
  </action>
  <verify>
    <automated>
      # Verify no 'as any' casts in router calls
      grep -n "s\.router.*as any\|: any = s\.router\|} as any)" functions/api/routes/judges.ts || echo "No 'as any' casts found"

      # Verify responseValidation enabled
      grep "responseValidation: true" functions/api/routes/judges.ts

      # Verify handlers use (input, c) pattern
      grep -E "async \((input|_input), c\)" functions/api/routes/judges.ts | wc -l

      # Verify TypeScript compiles
      npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "judges\.ts" | head -5 || echo "No type errors"
    </automated>
  </verify>
  <done>
    judges.ts has:
    - AppRouteInput imported
    - All handlers using `(input, c)` or `(_input, c)` signature without explicit types
    - Zero `as any` casts in router setup (lines 31, 208)
    - Response validation enabled with error handler
    - TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate users.ts to contract inference</name>
  <files>
    functions/api/routes/users.ts
  </files>
  <action>
For users.ts:

1. Add import: `import type { AppRouteInput } from "../../../shared/types/contracts";`

2. Update all handler signatures to use inferred types (handlers: getUsers, adminDetail, patchUser, updateUserProfile):
   - Change `async ({ query: _query }: { query: z.infer<typeof ...> }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Change `async ({ params }: { params: z.infer<typeof ...> }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Change `async ({ params, body }: { params: z.infer<typeof ...>, body: z.infer<typeof ...> }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Update destructuring: `const { field } = input.body;` or `const { id } = input.params;`

3. Remove `as any` cast from router setup:
   - Line 14: Change `const userTsRestRouter: any = s.router(userContract, {` to `const userTsRestRouter = s.router(userContract, {`

4. Enable response validation in createHonoEndpoints call (find the createHonoEndpoints call near end of file):
   ```typescript
   createHonoEndpoints(
     userContract,
     userTsRestRouter,
     usersRouter,
     {
       responseValidation: true,
       responseValidationErrorHandler: (err, _c) => {
         console.error('[Contract] Response validation failed:', err.cause);
         return { error: { message: 'Internal server error' }, status: 500 };
       }
     }
   );
   ```

5. Run TypeScript check: `npx tsc --noEmit` to verify no type errors
  </action>
  <verify>
    <automated>
      # Verify no 'as any' casts in router calls
      grep -n "s\.router.*as any\|: any = s\.router\|} as any)" functions/api/routes/users.ts || echo "No 'as any' casts found"

      # Verify responseValidation enabled
      grep "responseValidation: true" functions/api/routes/users.ts

      # Verify handlers use (input, c) pattern
      grep -E "async \((input|_input), c\)" functions/api/routes/users.ts | wc -l

      # Verify TypeScript compiles
      npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "users\.ts" | head -5 || echo "No type errors"
    </automated>
  </verify>
  <done>
    users.ts has:
    - AppRouteInput imported
    - All handlers using `(input, c)` signature without explicit types
    - Zero `as any` casts in router setup
    - Response validation enabled with error handler
    - TypeScript compilation passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted request data crosses here (validated by Zod in contracts) |
| handler→database | Trusted internal data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.1-04 | Tampering | judges.ts, users.ts | mitigate | Runtime Zod validation via ts-rest-hono contract inference |
| T-29.1-05 | Information Disclosure | judges.ts, users.ts | mitigate | Response validation prevents accidental data leakage via generic error handler |
| T-29.1-06 | Elevation of Privilege | users.ts (role changes) | mitigate | Existing ensureAdmin middleware + audit logging preserved |
</threat_model>

<verification>
## Overall Phase Checks

1. Both files import AppRouteInput from shared/types/contracts
2. All handler signatures use `(input, c)` without explicit type annotations
3. Zero `as any` casts in router setup (verified via grep)
4. Response validation enabled on both files
5. TypeScript compilation passes with no errors
6. Existing tests continue to pass (behavior unchanged)
</verification>

<success_criteria>
1. Zero `as any` casts in judges.ts and users.ts
2. Both files have `responseValidation: true` with error handler
3. All handlers use inferred `(input, c)` signature
4. TypeScript compilation passes
5. No functional changes to API behavior
</success_criteria>

<output>
After completion, create `.planning/phases/29.1-contract-inference-remaining/29.1-02-SUMMARY.md`
</output>
