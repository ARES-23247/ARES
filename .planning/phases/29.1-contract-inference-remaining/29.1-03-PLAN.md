---
phase: 29.1-contract-inference-remaining
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/api/routes/posts.ts
  - functions/api/routes/zulip.ts
autonomous: true
requirements:
  - ANTI_PATTERN_4
  - CONTRACT_INFERENCE

must_haves:
  truths:
    - "posts.ts uses inferred contract types with zero `as any` casts"
    - "zulip.ts uses inferred contract types with zero `as any` casts"
    - "Both files enable runtime response validation"
    - "Handler signatures use inferred `(input, c)` pattern without explicit type annotations"
  artifacts:
    - path: "functions/api/routes/posts.ts"
      provides: "Posts API with contract inference"
      contains: "s.router(postContract, postTsRestRouterObj)"
    - path: "functions/api/routes/zulip.ts"
      provides: "Zulip API with contract inference"
      contains: "s.router(zulipContract, zulipTsRestRouter)"
  key_links:
    - from: "both route files"
      to: "shared/types/contracts.ts"
      via: "AppRouteInput import"
      pattern: "import.*AppRouteInput.*from.*shared/types"
    - from: "both route files"
      to: "shared/schemas/contracts"
      via: "Contract type inference"
      pattern: "s.router\\(.*Contract, .*Router"
---

<objective>
Migrate posts.ts and zulip.ts to ts-rest contract inference, eliminating `as any` casts and enabling runtime response validation.

Purpose: Apply the proven Phase 29 migration pattern to posts.ts and zulip.ts. These files have complex handler patterns (posts.ts has FTS search and social posting, zulip.ts has message sync) but follow the same contract inference pattern.

Output: Both files using contract inference with zero `as any` casts and response validation enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-contract-inference/29-01-SUMMARY.md
@.planning/phases/29-contract-inference/29-02-SUMMARY.md
@shared/types/contracts.ts
@shared/types/api.ts
@shared/schemas/contracts/postContract.ts
@shared/schemas/contracts/zulipContract.ts
@functions/api/routes/analytics.ts
</context>

<interfaces>
<!-- Key types and contracts from Phase 29 infrastructure -->

From shared/types/contracts.ts:
```typescript
export type { AppRouteImplementation, AppRouteInput, ServerInferResponses, RecursiveRouterObj } from "ts-rest-hono";
```

From shared/schemas/contracts (contracts already exist):
```typescript
export const postContract = c.router({ /* ... */ });
export const zulipContract = c.router({ /* ... */ });
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate posts.ts to contract inference</name>
  <files>
    functions/api/routes/posts.ts
  </files>
  <action>
For posts.ts:

1. Add import: `import type { AppRouteInput } from "../../../shared/types/contracts";`

2. Update all handler signatures to use inferred types (find all handlers in postTsRestRouterObj):
   - Change `async (input: { query: z.infer<typeof ...> }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Change `async (input: { params: z.infer<typeof ...> }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Change `async ({ body }: { body: z.infer<typeof ...> }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Update destructuring: `const { field } = input.body;` or `const { slug } = input.params;` or `const { q } = input.query;`

3. Remove `as any` cast from router setup:
   - Line 38: Change `const postTsRestRouterObj: any = {` to `const postTsRestRouterObj = {`

4. Enable response validation in createHonoEndpoints call (find near end of file):
   ```typescript
   createHonoEndpoints(
     postContract,
     postTsRestRouter,
     postsRouter,
     {
       responseValidation: true,
       responseValidationErrorHandler: (err, _c) => {
         console.error('[Contract] Response validation failed:', err.cause);
         return { error: { message: 'Internal server error' }, status: 500 };
       }
     }
   );
   ```

5. Run TypeScript check: `npx tsc --noEmit` to verify no type errors
  </action>
  <verify>
    <automated>
      # Verify no 'as any' casts in router object
      grep -n "postTsRestRouterObj: any\|: any = " functions/api/routes/posts.ts | head -5 || echo "No 'as any' casts found"

      # Verify responseValidation enabled
      grep "responseValidation: true" functions/api/routes/posts.ts

      # Verify handlers use (input, c) pattern (count should be > 5)
      grep -E "async \(input, c\)" functions/api/routes/posts.ts | wc -l

      # Verify TypeScript compiles
      npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "posts\.ts" | head -5 || echo "No type errors"
    </automated>
  </verify>
  <done>
    posts.ts has:
    - AppRouteInput imported
    - All handlers using `(input, c)` signature without explicit types
    - Zero `as any` casts in router setup
    - Response validation enabled with error handler
    - TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate zulip.ts to contract inference</name>
  <files>
    functions/api/routes/zulip.ts
  </files>
  <action>
For zulip.ts:

1. Add import: `import type { AppRouteInput } from "../../../shared/types/contracts";`

2. Update all handler signatures to use inferred types:
   - Change `async ({ body }: { body: any }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Change `async ({ query }: { query: any }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Change `async ({ params }: { params: any }, c: Context<AppEnv>) =>` to `async (input, c) =>`
   - Update destructuring: `const { field } = input.body;` instead of `const { field } = body;`

3. Remove `as any` cast from router setup:
   - Find the router definition and remove `: any` type annotation

4. Enable response validation in createHonoEndpoints call:
   ```typescript
   createHonoEndpoints(
     zulipContract,
     zulipTsRestRouter,
     zulipRouter,
     {
       responseValidation: true,
       responseValidationErrorHandler: (err, _c) => {
         console.error('[Contract] Response validation failed:', err.cause);
         return { error: { message: 'Internal server error' }, status: 500 };
       }
     }
   );
   ```

5. Run TypeScript check: `npx tsc --noEmit` to verify no type errors
  </action>
  <verify>
    <automated>
      # Verify no 'as any' casts in router calls
      grep -n "s\.router.*as any\|: any = s\.router\|zulipTsRestRouter: any" functions/api/routes/zulip.ts || echo "No 'as any' casts found"

      # Verify responseValidation enabled
      grep "responseValidation: true" functions/api/routes/zulip.ts

      # Verify handlers use (input, c) pattern
      grep -E "async \(input, c\)" functions/api/routes/zulip.ts | wc -l

      # Verify TypeScript compiles
      npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "zulip\.ts" | head -5 || echo "No type errors"
    </automated>
  </verify>
  <done>
    zulip.ts has:
    - AppRouteInput imported
    - All handlers using `(input, c)` signature without explicit types
    - Zero `as any` casts in router setup
    - Response validation enabled with error handler
    - TypeScript compilation passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted request data crosses here (validated by Zod in contracts) |
| handler→database | Trusted internal data flow |
| API→Zulip | External service integration (trusted outbound) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.1-07 | Tampering | posts.ts, zulip.ts | mitigate | Runtime Zod validation via ts-rest-hono contract inference |
| T-29.1-08 | Information Disclosure | posts.ts, zulip.ts | mitigate | Response validation prevents accidental data leakage via generic error handler |
| T-29.1-09 | Spoofing | zulip.ts (Zulip API) | accept | Zulip API uses existing authentication; type safety internal only |
</threat_model>

<verification>
## Overall Phase Checks

1. Both files import AppRouteInput from shared/types/contracts
2. All handler signatures use `(input, c)` without explicit type annotations
3. Zero `as any` casts in router setup (verified via grep)
4. Response validation enabled on both files
5. TypeScript compilation passes with no errors
6. Existing tests continue to pass (behavior unchanged)
</verification>

<success_criteria>
1. Zero `as any` casts in posts.ts and zulip.ts
2. Both files have `responseValidation: true` with error handler
3. All handlers use inferred `(input, c)` signature
4. TypeScript compilation passes
5. No functional changes to API behavior
</success_criteria>

<output>
After completion, create `.planning/phases/29.1-contract-inference-remaining/29.1-03-SUMMARY.md`
</output>
