---
phase: 31-frontend-components
plan: 03
type: execute
wave: 2
depends_on:
  - 29-02
files_modified:
  - src/components/FinanceManager.tsx
  - src/pages/Blog.tsx
autonomous: true
requirements:
  - COMP-06
user_setup: []

must_haves:
  truths:
    - "API mutation payloads use proper contract types"
    - "API response types use inferred contract types"
    - "No `as any` type assertions for API calls"
  artifacts:
    - path: "src/components/FinanceManager.tsx"
      provides: "Typed mutation payloads"
      min_lines: 252
    - path: "src/pages/Blog.tsx"
      provides: "Typed API responses"
      min_lines: 23
  key_links:
    - from: "FinanceManager.tsx"
      to: "shared/schemas/contracts"
      via: "import contract types for lead mutations"
      pattern: "savePipeline\\.mutate\\(.*body:"
    - from: "Blog.tsx"
      to: "shared/schemas/contracts"
      via: "import inferred Post types"
      pattern: "api\\.posts\\.getPosts"
---

<objective>
Eliminate `as any` type assertions for API payloads and responses in FinanceManager and Blog components using ts-rest contract inference.

Purpose: Use the contract types exported in Phase 29 to properly type API mutations and queries, removing the need for `as any` assertions.

Output: Two component files using proper contract types from shared schemas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29/29-02-SUMMARY.md
@.planning/phases/31/31-RESEARCH.md
@shared/schemas/contracts/index.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Phase 29 exports (shared/schemas/contracts/index.ts):
```typescript
export * from './analyticsContract';
export * from './blogContract';
export * from './financeContract';
// ... other contracts
```

From existing FinanceManager.tsx (lines 253-254):
```typescript
// Current pattern:
savePipeline.mutate({ body: { ...item, status: update.status } as any });
```

From existing Blog.tsx (line 26-27):
```typescript
// Current pattern:
const rawBody = (postsRes as any)?.body;
const posts = postsRes?.status === 200 ? ... : [];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix FinanceManager mutation types</name>
  <files>src/components/FinanceManager.tsx</files>
  <action>Update FinanceManager.tsx to use proper contract types for mutations:

1. Examine the component to identify which API contract it uses (likely `financeContract` from shared schemas)

2. Add import for contract types at the top:
   ```typescript
   import type { InsertableLead } from "shared/schemas/contracts";
   ```
   OR use the actual contract type name if different (verify from shared/schemas/contracts/financeContract.ts)

3. Replace both `as any` assertions (lines 254 and 284):
   - Line 254: `savePipeline.mutate({ body: { ...item, status: update.status } });`
   - Line 284: `await savePipeline.mutateAsync({ body: { ...editingLead, ...updates } });`
   - Remove the `as any` suffix
   - Ensure the spread objects match the contract's expected body type

4. If TypeScript errors indicate a type mismatch, create a proper type for the mutation body by:
   - Reading the contract definition from `shared/schemas/contracts/financeContract.ts`
   - Creating a local type alias if needed: `type LeadUpdate = { ... };`
   - Do NOT use `as any` - fix the type instead

DO NOT modify the mutation logic itself, only the types.
</action>
  <verify>
    <automated>grep -c "as any" src/components/FinanceManager.tsx | grep -q "^0$" || grep "financeContract" src/components/FinanceManager.tsx</automated>
  </verify>
  <done>FinanceManager mutations use proper contract types with zero `as any` assertions</done>
</task>

<task type="auto">
  <name>Task 2: Fix Blog API response types</name>
  <files>src/pages/Blog.tsx</files>
  <action>Update Blog.tsx to use proper contract types for API responses:

1. Check the blog contract from `shared/schemas/contracts/blogContract.ts` for the expected response shape

2. Add import for contract types if needed:
   ```typescript
   import type { Post } from "shared/schemas/contracts";
   ```
   OR use the actual type name from the contract

3. Replace the `as any` assertion and response handling (lines 26-27):
   - Current: `const rawBody = (postsRes as any)?.body;`
   - Update to use proper ts-rest response typing:
     ```typescript
     const posts = postsRes?.status === 200 ? postsRes.body : [];
     ```
   - The `body` property should already be typed from the contract

4. Update the map function type annotation (line 59):
   - Remove `(post: any)` and use the proper type: `(post: Post)` or the type from the contract

5. If TypeScript errors indicate the response shape doesn't match, verify:
   - The contract definition matches the actual API response
   - The posts array might be nested in a `posts` property
   - Update the type to match reality, not force with `as any`

DO NOT modify the rendering logic, only the type annotations.
</action>
  <verify>
    <automated>grep -c "as any" src/pages/Blog.tsx | grep -q "^0$" && grep "post:" src/pages/Blog.tsx | grep -v "any"</automated>
  </verify>
  <done>Blog.tsx uses proper contract types for API responses with zero `as any` assertions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → Component | Data from backend API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | T | FinanceManager | accept | Data validation happens at API boundary (Phase 29) |
| T-31-06 | I | Blog.tsx | accept | Post data is public, already validated |

No mitigations required - contract types provide type safety but data is already validated at the API boundary.
</threat_model>

<verification>
1. TypeScript compilation succeeds: `npx tsc --noEmit`
2. No `@typescript-eslint/no-explicit-any` violations in modified files
3. Contract types are imported and used
4. Components still render correctly
</verification>

<success_criteria>
1. Zero `as any` violations in FinanceManager.tsx
2. Zero `as any` violations in Blog.tsx
3. Contract types properly imported
4. Response handling preserves existing logic
</success_criteria>

<output>
After completion, create `.planning/phases/31/31-03-SUMMARY.md`
</output>
