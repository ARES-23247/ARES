---
phase: 08-deferred-items-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md
  - functions/utils/zulipSync.ts
  - src/components/AdminUsers.tsx
autonomous: false
requirements:
  - INV-01
user_setup: []
must_haves:
  truths:
    - "All ARESWEB users with verified emails have corresponding Zulip accounts"
    - "Any missing Zulip accounts are documented with user emails"
    - "Sync process between aresfirst.org and Zulip is understood"
    - "Fix plan exists if sync gaps are found"
  artifacts:
    - path: ".planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md"
      provides: "Investigation findings and gap analysis"
      min_lines: 100
    - path: "functions/utils/zulipSync.ts"
      provides: "Zulip sync utility functions"
      exports: ["auditMissingUsers", "inviteUsers"]
    - path: "src/components/AdminUsers.tsx"
      provides: "Admin UI for Zulip audit and invite"
      contains: "auditMutation|inviteMutation"
  key_links:
    - from: "src/components/AdminUsers.tsx"
      to: "api.zulip.auditMissingUsers"
      via: "Mutation hook invocation"
      pattern: "api\\.zulip\\.auditMissingUsers\\.useMutation"
    - from: "src/components/AdminUsers.tsx"
      to: "api.zulip.inviteUsers"
      via: "Mutation hook invocation"
      pattern: "api\\.zulip\\.inviteUsers\\.useMutation"
    - from: "Zulip API"
      to: "aresfirst.zulipchat.com"
      via: "External API calls"
      pattern: "zulipchat\\.com/api"
---

<objective>
Investigate Zulip account sync between aresfirst.org and Zulip, verify all team members have linked accounts, document any gaps, and create a fix plan if issues are found.

Purpose: INV-01 from v6.0 noted investigation — ensure all ARESWEB users can access Zulip for team communication and identify any sync gaps.

Output: Investigation document with findings and any necessary remediation steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-deferred-items-cleanup/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@functions/utils/zulipSync.ts
@functions/api/routes/zulip.ts
@shared/schemas/contracts/zulipContract.ts
@src/components/AdminUsers.tsx
@.agents/skills/aresweb-zero-trust-security/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Audit existing Zulip integration code and API contracts</name>
  <files>functions/utils/zulipSync.ts, functions/api/routes/zulip.ts, shared/schemas/contracts/zulipContract.ts</files>
  <action>
    Review the existing Zulip integration to understand the current sync implementation:

    1. Check zulipSync.ts for:
       - How Zulip API calls are made (sendZulipMessage, updateZulipMessage, deleteZulipMessage)
       - Authentication method (Basic auth with email + API key)
       - Base URL: https://aresfirst.zulipchat.com/api/v1

    2. Check zulip.ts (API route) for:
       - What endpoints are exposed
       - Whether auditMissingUsers and inviteUsers handlers exist
       - How the handlers interact with Zulip API

    3. Check zulipContract.ts for:
       - What contract methods are defined
       - Request/response schemas for audit and invite operations

    4. Check AdminUsers.tsx for:
       - How the Zulip audit button is wired (lines 283-291)
       - The auditMutation and inviteMutation implementations (lines 134-153)
       - What the Zulip audit modal displays (lines 448-494)

    Document the current state in 08-03-ZULIP-INVESTIGATION.md:
    - What sync mechanisms exist
    - What API endpoints are available
    - What the admin UI can do
    - Any obvious gaps or missing functionality
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && ls -la .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md 2>&1</automated>
  </verify>
  <done>
    - Investigation document created with current state analysis
    - Existing Zulip integration capabilities documented
    - Identified any missing pieces
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Check for existing Zulip API endpoints and compare to contract</name>
  <files>functions/api/routes/zulip.ts, shared/schemas/contracts/zulipContract.ts</files>
  <action>
    Verify that the Zulip API routes match the contract expectations.

    Read functions/api/routes/zulip.ts and check:
    1. Is there an endpoint for /api/zulip/admin/audit-users?
    2. Is there an endpoint for /api/zulip/admin/invite-users?
    3. What do these endpoints return?

    Read shared/schemas/contracts/zulipContract.ts and check:
    1. Is auditMissingUsers defined in the contract?
    2. Is inviteUsers defined in the contract?
    3. What are the expected request/response shapes?

    Compare the two:
    - Does the route implementation match the contract?
    - Are any contract methods missing from the route handler?
    - Are any route handlers missing from the contract?

    Document findings in 08-03-ZULIP-INVESTIGATION.md under "Contract vs Implementation Gap Analysis".
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && grep -n "audit\|invite" functions/api/routes/zulip.ts shared/schemas/contracts/zulipContract.ts 2>&1</automated>
  </verify>
  <done>
    - Contract vs implementation comparison completed
    - Any gaps documented
    - Missing endpoints or contract methods identified
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide on investigation approach based on existing code</name>
  <files>.planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md</files>
  <action>
    Based on the findings from Tasks 1 and 2, decide on the next steps:

    If audit/invite endpoints already exist:
    - Proceed to Task 4 to run a manual audit against the real Zulip instance
    - Document how to use the existing endpoints

    If endpoints are missing:
    - Document what needs to be implemented
    - Create implementation tasks for the missing pieces

    If endpoints exist but have issues:
    - Document the specific issues
    - Create fix tasks

    Update 08-03-ZULIP-INVESTIGATION.md with:
    - "Investigation Approach" section
    - Required actions (if any)
    - Decision rationale
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && grep -n "Investigation Approach\|Required Actions" .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md 2>&1</automated>
  </verify>
  <done>
    - Investigation approach documented
    - Decision made on next steps
    - Required actions listed (if any)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Run Zulip user audit (manual verification step)</name>
  <files>.planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md</files>
  <action>
    If the audit endpoint exists, run a manual audit:

    1. Access the ARESWEB admin dashboard at /dashboard/users (requires admin auth)
    2. Click the "Audit Zulip" button (if present)
    3. Document the results:
       - How many users were audited?
       - How many users are missing from Zulip?
       - Which users (emails) are missing?

    If the audit endpoint does not exist or cannot be accessed:
    - Document the blocker
    - Propose an alternative approach (e.g., manual comparison of user lists)

    Document findings in 08-03-ZULIP-INVESTIGATION.md under "Audit Results".

    Note: This task requires human action because it involves:
    - Authenticating to the production ARESWEB admin panel
    - Accessing Zulip admin credentials (if needed)
    - Viewing potentially sensitive user email lists
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && grep -n "Audit Results\|Missing Users" .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md 2>&1</automated>
  </verify>
  <done>
    - Audit results documented
    - Missing Zulip users listed (if any)
    - Sync status confirmed
  </done>
</task>

<task type="auto">
  <name>Task 5: Document findings and create fix plan (if needed)</name>
  <files>.planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md</files>
  <action>
    Create a comprehensive investigation report in 08-03-ZULIP-INVESTIGATION.md with the following sections:

    1. **Executive Summary**
       - Investigation date
       - Overall sync status (healthy / needs attention)
       - Number of users audited
       - Number of missing Zulip accounts

    2. **Current State**
       - Existing Zulip integration components
       - API endpoints available
       - Admin UI capabilities

    3. **Audit Findings**
       - Total ARESWEB users
       - Users with linked Zulip accounts
       - Users missing from Zulip (list emails)
       - Any sync errors or issues

    4. **Gap Analysis**
       - What functionality is missing (if anything)
       - What needs to be implemented
       - Known issues or limitations

    5. **Recommended Actions** (if gaps found)
       - Implementation tasks for missing endpoints
       - Manual sync steps needed
       - Automation opportunities

    6. **Fix Plan** (if issues found)
       - Specific steps to resolve sync gaps
       - Estimated effort
       - Dependencies

    If no issues found:
    - Document that sync is healthy
    - Recommend periodic audit schedule
    - Note any preventive measures

    Format the document in markdown with clear headings and bullet points.
    Keep 8th-grade reading level in mind per aresweb-documentation-readability/SKILL.md.
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && wc -l .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md && grep -n "Executive Summary\|Audit Findings\|Recommended Actions" .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md</automated>
  </verify>
  <done>
    - Comprehensive investigation document created
    - All sections complete with findings
    - Fix plan documented (if issues found)
    - Document is clear and actionable
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARESWEB → Zulip API | External API calls to aresfirst.zulipchat.com |
| Admin → Audit endpoint | Admin-only access to user email lists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | S | Zulip API credentials | mitigate | API key stored in Cloudflare environment variables, not in code |
| T-08-10 | I | User email exposure | mitigate | Admin-only endpoint protected by ensureAdmin middleware |
| T-08-11 | D | Zulip service availability | accept | External service; audit can be retried if Zulip is down |
| T-08-12 | T | Audit report data | mitigate | Investigation document stored in .planning/ (not committed if sensitive) |
</threat_model>

<verification>
## Overall Phase Checks

1. Zulip integration codebase has been audited
2. Contract vs implementation gaps identified (if any)
3. Audit has been run (or blocker documented)
4. Findings documented in investigation report
5. Fix plan created (if issues found)
6. Investigation document is clear and actionable

## Verification Commands

```bash
# Check investigation document exists
ls -la .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md

# Check document has required sections
grep -n "Executive Summary\|Audit Findings\|Recommended Actions" \
  .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md

# Check document is not empty
wc -l .planning/phases/08-deferred-items-cleanup/08-03-ZULIP-INVESTIGATION.md
```
</verification>

<success_criteria>
1. 08-03-ZULIP-INVESTIGATION.md document created with all required sections
2. Current state of Zulip integration documented
3. Audit results recorded (or blocker documented with rationale)
4. Gaps and issues identified (if any exist)
5. Fix plan created with actionable steps (if needed)
6. Document is clear, concise, and follows 8th-grade readability standards
</success_criteria>

<output>
After completion, create `.planning/phases/08-deferred-items-cleanup/08-03-SUMMARY.md`
</output>
