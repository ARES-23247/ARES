---
phase: 08-deferred-items-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/media.spec.ts
autonomous: true
requirements:
  - DEF-01
user_setup: []
must_haves:
  truths:
    - "Media manager upload flow is testable via E2E"
    - "Media manager delete operation is testable via E2E"
    - "Media manager gallery view is testable via E2E"
    - "Media manager filter/search is testable via E2E"
    - "All tests pass via Playwright test runner"
  artifacts:
    - path: "tests/e2e/media.spec.ts"
      provides: "E2E test suite for media manager operations"
      min_lines: 100
    - path: "tests/e2e/media-manager.spec.ts"
      provides: "Additional E2E tests for full media manager UI"
      min_lines: 80
  key_links:
    - from: "tests/e2e/media.spec.ts"
      to: "src/components/AssetManager.tsx"
      via: "Playwright UI selectors matching component structure"
      pattern: "getByRole\\(.*button.*\\)|getByText\\(.*\\)"
    - from: "tests/e2e/media.spec.ts"
      to: "/api/media/admin"
      via: "API route mocking for upload/delete operations"
      pattern: "page.route.*api/media"
---

<objective>
Create comprehensive E2E tests for the Media Manager (Asset Vault) using Playwright, covering upload, delete, gallery view, and filter/search operations.

Purpose: DEF-01 from v5.7 deferred items — ensure media manager operations are validated end-to-end following existing E2E patterns (kanban.spec.ts, collaboration.spec.ts).

Output: Updated and new E2E test files covering all media manager CRUD operations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-deferred-items-cleanup/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@tests/e2e/kanban.spec.ts
@tests/e2e/collaboration.spec.ts
@tests/e2e/media.spec.ts
@src/components/AssetManager.tsx
@src/components/assets/AssetUploader.tsx
@src/components/assets/AssetGrid.tsx
@src/hooks/useMedia.ts
@shared/schemas/contracts/mediaContract.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand existing media.spec.ts with upload flow E2E test</name>
  <files>tests/e2e/media.spec.ts</files>
  <behavior>
    Test 1: Navigate to /dashboard/assets and verify Asset Vault title is visible
    Test 2: Mock authentication as admin user (following kanban.spec.ts pattern)
    Test 3: Mock /api/media/admin GET route to return empty or sample assets array
    Test 4: Verify "All Assets" filter button exists and is clickable
    Test 5: Click upload button, verify file input dialog triggers
  </behavior>
  <action>
    Add a new test case "Asset Vault loads and displays upload interface" to tests/e2e/media.spec.ts.
    
    Follow the authentication mocking pattern from kanban.spec.ts (lines 6-61):
    - Mock /api/auth/get-session with admin user
    - Mock /profile/me with admin profile
    - Add better-auth.session_token cookie
    - Set __PLAYWRIGHT_TEST__ flag via addInitScript
    
    Mock the media API:
    - Route **/api/media/admin to return { media: [] } or sample asset array
    - Each asset should have: key, size, uploaded, url, folder properties per assetSchema
    
    Navigate to /dashboard/assets and verify:
    - Heading with "Asset Vault" text is visible
    - "All Assets" button exists (selector: button with text "All Assets")
    - Upload button exists (selector: label[for="asset-upload-input"] or button with "Upload")
    - Folder/tag input field exists (selector: input with placeholder "Assign Tag/Folder")
    
    Use existing test structure in media.spec.ts — this is an expansion, not a rewrite.
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && npm test -- tests/e2e/media.spec.ts --project=chromium</automated>
  </verify>
  <done>
    - New test case added to media.spec.ts
    - Test verifies Asset Vault UI renders correctly
    - Authentication mocking follows existing patterns
    - Test passes locally
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add media upload E2E test with multipart form handling</name>
  <files>tests/e2e/media.spec.ts</files>
  <behavior>
    Test 1: Mock POST /api/media/admin/upload to accept file uploads
    Test 2: Trigger file upload via AssetUploader component
    Test 3: Verify upload success response is handled correctly
    Test 4: Verify toast notification appears on successful upload
    Test 5: Verify gallery refreshes after upload (query invalidation)
  </behavior>
  <action>
    Add test case "Media upload completes successfully via AssetUploader" to media.spec.ts.
    
    Mock setup:
    - Extend existing beforeEach with /api/media/admin/upload POST route
    - Return { success: true, key: "test-folder/test-image.png", url: "/api/media/test-folder/test-image.png" }
    
    Test flow:
    1. Navigate to /dashboard/assets
    2. Set folder input value to "E2E-Test-Folder" (fill input with placeholder "Assign Tag/Folder")
    3. Trigger file input change — create a mock File object and dispatch change event on input#asset-upload-input
    4. Wait for upload completion (use Promise.race with timeout or waitForResponse)
    5. Verify success state — check for toast message or upload button state change
    
    Note: The actual file upload compression happens in useMedia.ts via compressImage.
    For E2E, we mock the API response — we're testing the UI flow, not image processing.
    
    Follow the API mocking pattern from collaboration.spec.ts (lines 74-134).
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && npm test -- tests/e2e/media.spec.ts --project=chromium</automated>
  </verify>
  <done>
    - Upload flow test added
    - Mock API correctly simulates successful upload
    - Test verifies UI updates after upload
    - Test passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add media delete and gallery filter E2E tests</name>
  <files>tests/e2e/media.spec.ts</files>
  <behavior>
    Test 1: Mock DELETE /api/media/admin/:key route
    Test 2: Click delete button on asset card, verify confirmation flow
    Test 3: Confirm delete, verify API is called and asset removed from UI
    Test 4: Test folder filter button functionality
    Test 5: Verify assets are filtered when folder button is clicked
  </behavior>
  <action>
    Add two test cases to media.spec.ts:
    
    1. "Media delete confirms and removes asset from gallery":
       - Mock GET /api/media/admin with sample assets array (3 assets across 2 folders)
       - Mock DELETE /api/media/admin/:key to return { success: true }
       - Navigate to /dashboard/assets
       - Find first asset card (use AssetGrid structure: div with group class)
       - Click "Delete" button (text selector)
       - Verify "Confirm" button appears (text changes from "Delete" to "Confirm")
       - Click confirm
       - Verify DELETE API was called
       - Verify asset no longer in DOM
    
    2. "Folder filter buttons correctly filter assets":
       - Mock GET /api/media/admin with assets in folders: "Gallery", "Blog", "Events"
       - Navigate to /dashboard/assets
       - Verify all assets visible initially
       - Click "Gallery" folder button
       - Verify only assets with folder="Gallery" are visible
       - Click "All Assets" button
       - Verify all assets visible again
    
    AssetGrid selectors (from AssetGrid.tsx lines 106-116):
    - Asset card: div.group.relative.bg-black (with aspect-square child)
    - Delete button: button with text "Delete" then "Confirm"
    - Folder badge: span.bg-white/5 with uppercase text
    
    Follow the button interaction pattern from kanban.spec.ts (lines 109-116).
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && npm test -- tests/e2e/media.spec.ts --project=chromium</automated>
  </verify>
  <done>
    - Delete flow test with confirmation pattern
    - Folder filter test with multiple mock assets
    - Tests verify DOM updates correctly
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Create media-manager.spec.ts for advanced scenarios</name>
  <files>tests/e2e/media-manager.spec.ts</files>
  <action>
    Create a new test file tests/e2e/media-manager.spec.ts for advanced media manager scenarios not covered in media.spec.ts.
    
    Test cases to include:
    
    1. "Asset search/filter keyboard navigation works":
       - Verify search input is focusable
       - Type search term, verify assets filter in real-time
       - Test keyboard navigation through asset grid
    
    2. "Copy URL button copies asset URL to clipboard":
       - Mock GET /api/media/admin with sample asset
       - Navigate to /dashboard/assets
       - Click "Copy URL" button on first asset
       - Verify clipboard contains correct URL format
       - Verify button text changes to "Copied!" temporarily
    
    3. "Move asset modal opens and submits":
       - Mock GET /api/media/admin with sample asset
       - Mock PUT /api/media/admin/move/:key to return { success: true, newKey: "..." }
       - Click "Move" button on asset
       - Verify modal appears (use ModalContext prompt pattern)
       - Enter new folder name
       - Submit and verify API call
    
    4. "Syndicate button opens broadcast modal":
       - Click "Broadcast" button on asset
       - Verify AssetSyndicateModal appears
       - Verify caption input exists
       - Test modal cancel/close
    
    Follow the file structure from collaboration.spec.ts (import test, expect; describe block; beforeEach for auth).
    
    Include accessibility audit using AxeBuilder (as seen in kanban.spec.ts lines 134-141):
    - Run after main interaction tests
    - Use tags: wcag2a, wcag2aa, wcag21a, wcag21aa
    - Disable duplicate-id rule if needed (dnd-kit pattern)
  </action>
  <verify>
    <automated>cd "C:\Users\david\dev\robotics\ftc\ARESWEB" && npm test -- tests/e2e/media-manager.spec.ts --project=chromium</automated>
  </verify>
  <done>
    - New media-manager.spec.ts file created
    - 4 advanced scenario tests implemented
    - Accessibility audit included
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Media upload/delete requests cross from browser to Cloudflare Functions |
| E2E test → Mock API | Playwright tests mock API responses; test data is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | S | Media upload endpoint | mitigate | E2E tests validate upload flow; actual security handled by ensureAdmin middleware in production |
| T-08-02 | I | Mock API responses | accept | E2E test environment only; mocks are isolated from production data |
| T-08-03 | D | Delete operations | mitigate | Tests verify confirmation flow; actual deletion requires admin auth (enforced by middleware) |
| T-08-04 | T | Asset URL exposure | accept | URLs are public R2 assets; no sensitive data in test mocks |
</threat_model>

<verification>
## Overall Phase Checks

1. All media manager CRUD operations have E2E coverage
2. Tests follow existing patterns (kanban.spec.ts, collaboration.spec.ts)
3. Authentication mocking is consistent across all tests
4. API route mocking uses correct response schemas from mediaContract
5. Accessibility audit passes for tested components
6. Tests run successfully with Playwright test runner

## Verification Commands

```bash
# Run all media-related E2E tests
npm test -- tests/e2e/media*.spec.ts --project=chromium

# Run with coverage (if configured)
npm test -- tests/e2e/media*.spec.ts --project=chromium --coverage

# Run specific test file
npm test -- tests/e2e/media-manager.spec.ts --project=chromium
```
</verification>

<success_criteria>
1. tests/e2e/media.spec.ts expanded with 3 new test cases (upload, delete, filter)
2. tests/e2e/media-manager.spec.ts created with 4 advanced scenario tests
3. All tests use consistent auth mocking pattern from existing E2E files
4. API mocks match mediaContract schema exactly
5. Accessibility audit included and passing
6. Playwright test runner reports 0 failures for media test suite
</success_criteria>

<output>
After completion, create `.planning/phases/08-deferred-items-cleanup/08-01-SUMMARY.md`
</output>
