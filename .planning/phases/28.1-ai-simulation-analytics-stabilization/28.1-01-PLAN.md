# Phase 28.1: AI Simulation & Analytics Stabilization

## Goal
Restore and stabilize the AI educational simulations and fix regressions in the analytics dashboard caused by recent metrics integration.

## Proposed Changes

### AI Simulation Loader
#### [MODIFY] [SimRunner.tsx](file:///c:/Users/david/dev/robotics/ftc/ARESWEB/src/pages/SimRunner.tsx)
- Correct the dynamic import path resolution to use the `folder` metadata property instead of the `id`.
- This ensures that kebab-case simulation folders (like `nn-activation`) are correctly found by the dynamic loader.

#### [MODIFY] [nn-biology/index.tsx](file:///c:/Users/david/dev/robotics/ftc/ARESWEB/src/sims/nn-biology/index.tsx)
- Fix missing `Code` icon import from `lucide-react`.

### Analytics Dashboard
#### [MODIFY] [AnalyticsDashboard.tsx](file:///c:/Users/david/dev/robotics/ftc/ARESWEB/src/components/AnalyticsDashboard.tsx)
- Add explicit type definitions to `reduce` and `map` functions to resolve `implicit any` errors.

#### [MODIFY] [useAcademy.ts](file:///c:/Users/david/dev/robotics/ftc/ARESWEB/src/hooks/useAcademy.ts)
- Correct the analytics category from `'academy'` to `'doc'` to match the `AnalyticsCategory` union type.

### Backend Resilience
#### [MODIFY] [analytics.ts (Backend)](file:///c:/Users/david/dev/robotics/ftc/ARESWEB/functions/api/routes/analytics.ts)
- Implement comprehensive `.catch()` handlers for all D1 database queries to ensure dashboard robustness if telemetry tables are empty.

## Verification Plan
- Run `npm run generate:sims` to update the registry.
- Run `npx tsc --noEmit` to verify all TypeScript errors are resolved.
- Verify simulation loading in the browser.
