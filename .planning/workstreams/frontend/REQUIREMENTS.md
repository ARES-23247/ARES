# Milestone v5.5 (Frontend) Requirements

## Core Editor Polish
- [x] **ED-01**: User can start a simulation from a "Blank Canvas" template instead of just pre-built physics templates.
- [x] **ED-02**: The AI generation stream properly overrides files instead of duplicating partial filenames on every chunk.
- [x] **ED-03**: The Babel compiler supports TypeScript non-null assertions (`!`) so that TSX templates don't crash with "Unexpected Token".
- [ ] **ED-04**: Implement resizable split panes and multiple tabs. (Deferred to Phase 2)
