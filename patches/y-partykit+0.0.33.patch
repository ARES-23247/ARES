diff --git a/node_modules/y-partykit/dist/chunk-2H6M6QIA.mjs b/node_modules/y-partykit/dist/chunk-2H6M6QIA.mjs
index a6b0541..6c20cb0 100644
--- a/node_modules/y-partykit/dist/chunk-2H6M6QIA.mjs
+++ b/node_modules/y-partykit/dist/chunk-2H6M6QIA.mjs
@@ -48,6 +48,11 @@ var handleChunked = (receive) => {
   let batch;
   let start;
   return (message) => {
+    if (message && typeof message === "object" && !("data" in message)) {
+      message = { data: message };
+    } else if (typeof message === "string") {
+      message = { data: message };
+    }
     if (typeof message === "string") {
       return;
     }
diff --git a/node_modules/y-partykit/dist/index.js b/node_modules/y-partykit/dist/index.js
index 9fdd260..2c38cbd 100644
--- a/node_modules/y-partykit/dist/index.js
+++ b/node_modules/y-partykit/dist/index.js
@@ -790,10 +790,15 @@ async function onConnect(conn, room, opts2 = {}) {
     "message",
     handleChunked((data) => {
       if (typeof data !== "string") {
+        console.log(`[YjsServer] Received binary message, length:`, data.byteLength || data.length);
+        console.log(`[YjsServer] data prototype:`, Object.prototype.toString.call(data));
+        const buf = new Uint8Array(data);
+        console.log(`[YjsServer] Uint8Array length:`, buf.length);
+        console.log(`[YjsServer] first 10 bytes:`, buf.slice(0, 10));
         return messageListener(
           conn,
           doc,
-          new Uint8Array(data),
+          buf,
           options.readOnly ?? false
         );
       } else {
diff --git a/node_modules/y-partykit/dist/index.mjs b/node_modules/y-partykit/dist/index.mjs
index 5c0b943..6534205 100644
--- a/node_modules/y-partykit/dist/index.mjs
+++ b/node_modules/y-partykit/dist/index.mjs
@@ -374,19 +374,27 @@ async function onConnect(conn, room, opts2 = {}) {
   const doc = await getYDocPromises.get(room.id);
   getYDocPromises.delete(room.id);
   doc.conns.set(conn, /* @__PURE__ */ new Set());
+  const chunkHandler = handleChunked((data) => {
+    if (typeof data !== "string") {
+      return messageListener(
+        conn,
+        doc,
+        new Uint8Array(data),
+        options.readOnly ?? false
+      );
+    } else {
+    }
+  });
+
   conn.addEventListener(
     "message",
-    handleChunked((data) => {
-      if (typeof data !== "string") {
-        return messageListener(
-          conn,
-          doc,
-          new Uint8Array(data),
-          options.readOnly ?? false
-        );
-      } else {
+    async (event) => {
+      let data = event.data || event;
+      if (typeof Blob !== "undefined" && data instanceof Blob) {
+        data = await data.arrayBuffer();
       }
-    })
+      chunkHandler({ data });
+    }
   );
   conn.addEventListener("close", () => {
     closeConn(doc, conn);
